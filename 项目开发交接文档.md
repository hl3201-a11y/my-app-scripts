# 食光盒 · 项目开发交接文档

> 本文档面向「接手本项目的下一位开发者 / AI」。即使对话记录丢失，仅凭本文 + 仓库文件即可完整理解项目并继续开发。
> 生成时间：2026-08-20。项目路径：`C:\Users\Administrator\WorkBuddy\2026-08-20-13-15-22\食光盒\`

---

## ① 项目目标与背景

**食光盒**是一款**本地优先（local-first）的家庭物品储存管理 App**，核心解决"家里东西放哪了、什么时候过期"的问题。

依据原始设计文档（`桌面/食光盒APP设计文档.docx`）提取的核心诉求：
- 记录物品（名称、数量、存放位置、放入时间、到期时间）
- 按位置（厨房/客厅/卧室…）归类与盘点
- 临期 / 过期提醒（只提醒、不自动删除）
- 每日日切快照（daily_log），形成历史趋势
- 回收桶（已处理/过期物品暂存，可恢复或彻底删除）
- 本地备份导出 / 导入（JSON）

**关键约束（用户硬性要求）**：启动快、占用空间小、数据增长也不额外占手机空间。
因此技术选型刻意避开重型原生框架（如文档原定的 Kotlin+Compose+Hilt+ML Kit，APK 数十 MB、冷启动慢），改为**零依赖纯前端 PWA**，必要时用 Capacitor 包成轻量原生安卓壳（复用系统 WebView，不引入额外运行时）。

---

## ② 技术栈与运行环境

| 层 | 技术 | 说明 |
|---|---|---|
| Web 应用 | 原生 HTML + CSS + JavaScript（**零运行时框架**） | 无 React/Vue，无打包器，直接 `<script>` 引入 |
| 本地存储 | IndexedDB（经自写封装 `db.js`） | 结构化数据，每条几百字节；应用壳体积恒定不随数据增长 |
| 离线/秒开 | Service Worker（`sw.js`，缓存优先） | **仅在 Web/PWA 模式生效**；原生壳内已禁用 |
| 照片 | `<canvas>` 压缩为 ≤480px JPEG(0.6)，存为 Blob | 单张约 15–40KB |
| 安卓原生壳 | **Capacitor 8.5.0**（`@capacitor/core` / `@capacitor/android` / `@capacitor/cli`） | 把 Web 资源打进原生安卓工程，运行在系统 WebView |
| 工具链 | Node.js 22.22.2 + npm 10.9.7 | 仅用于 Capacitor 命令与资源同步，不进产物 |

**依赖版本（已锁定在 `package.json` / `node_modules`）**
- @capacitor/core 8.5.0
- @capacitor/android 8.5.0
- @capacitor/cli 8.5.0
- Android 侧：`minSdkVersion = 24`（Android 7.0+），`compileSdkVersion = 36`，`targetSdkVersion = 36`，androidxAppCompat 1.7.1

**运行环境注意**
- 开发/构建当前环境**没有安装 Android SDK**，因此**无法在此处编译出 `.apk`**。安卓工程的生成、配置、资源同步已全部完成，最终编译需在用户本机（Android Studio 或 Android SDK + Gradle）执行，详见 ⑧。
- Web 版直接由任意静态服务器托管即可（如 `python -m http.server`）。
- 浏览器中相机/语音（Web Speech / getUserMedia）需要 **HTTPS 或 localhost**；局域网 http 下可用「选图」替代拍照。

---

## ③ 系统架构与文件目录说明

```
食光盒/
├─ index.html              # PWA 入口（引用下面 3 个脚本与 styles.css）
├─ manifest.webmanifest    # PWA 清单（名称/图标/可安装）
├─ sw.js                   # Service Worker（缓存应用壳，Web 模式）
├─ styles.css              # 全部样式（已做视觉升级，纯 CSS 零依赖）  —— 见 assets/styles.css 实为同内容
├─ assets/
│  ├─ styles.css           # 真实样式文件（index.html 引用此路径）
│  ├─ db.js                # IndexedDB 数据层（open/seed/ CRUD/批量）
│  ├─ shelf.js             # 业务辅助（品类 emoji、到期"智能建议"映射）
│  ├─ app.js               # UI 与交互主逻辑（渲染各页面、事件、备份、日切）
│  ├─ icon192.png / icon512.png  # PWA 图标（极小 PNG，脚本生成）
├─ www/                    # Capacitor 打包用的"干净"web 资源副本（由 build-www.js 生成）
│  ├─ index.html / manifest.webmanifest / sw.js / assets/
├─ build-www.js            # 把根目录源文件同步进 www/ 的脚本
├─ capacitor.config.json   # Capacitor 配置（appId / appName / webDir / androidScheme）
├─ package.json            # npm 脚本（www / sync / android）
├─ android/                # 由 `cap add android` 生成的原生安卓工程（已修正可直接构建）
│  └─ app/src/main/
│     ├─ java/com/shiguanghe/app/MainActivity.java
│     ├─ res/values/strings.xml   # app_name=食光盒
│     ├─ assets/public/           # 打包进 APK 的 web 资源（已同步）
│     └─ AndroidManifest.xml
├─ preview.html            # 单文件自包含预览版（内联 CSS+JS），用于即时演示
└─ 项目开发交接文档.md      # 本文档
```

**数据模型（IndexedDB，库名 `shiguanghe`）**
- `locations`：{name, type(0预置/1自定义), sort} —— 预置 8 个家庭分区，首次启动原子播种
- `items`：{name, qty, locationId, putTime, expireAt(可空=长期), photoBlobs?[]}
- `archive`：{original(物品快照), archivedAt, reason} —— 回收桶
- `daily_log`：{date, summary{total,expiring,expired}, snapshot[]} —— 每日日切
- `meta`：key-value（如 `settings`）

**状态规则（`app.js` 的 `statusOf`）**：`expireAt` 为空→长期；已过期→🔴；距今天数 ≤ `advanceDays`(默认3)→⚠️快到期；否则✅正常。提醒只提示不删除。

---

## ④ 完整源代码（文件清单 + 关键文件全文）

> 说明：Web 业务源码（`db.js` / `shelf.js` / `app.js` / `styles.css`）已作为独立文件存在于仓库，逐文件阅读即可获得**全部内容**；此处把"胶水/配置类"小文件全文贴出，大文件给出路径与职责，避免重复。

### 4.1 `capacitor.config.json`（全文）
```json
{
  "appId": "com.shiguanghe.app",
  "appName": "食光盒",
  "webDir": "www",
  "server": {
    "androidScheme": "https"
  }
}
```

### 4.2 `package.json`（全文）
```json
{
  "name": "shiguanghe",
  "version": "1.0.0",
  "private": true,
  "description": "食光盒 - 本地优先的物品储存管理 App（PWA + Capacitor 安卓壳）",
  "scripts": {
    "www": "node build-www.js",
    "sync": "node build-www.js && cap sync",
    "android": "node build-www.js && cap sync && cap open android"
  }
}
```

### 4.3 `build-www.js`（全文）
```javascript
/* 把源文件同步到 www/（Capacitor 打包用的干净 web 资源目录）
 * 用法：node build-www.js
 * 之后 npm run sync 会先同步再 cap sync 把资源拷进安卓工程。 */
const fs = require('fs');
const path = require('path');

const root = __dirname;
const dst = path.join(root, 'www');
if (!fs.existsSync(dst)) fs.mkdirSync(dst, { recursive: true });

// 顶层文件
['index.html', 'manifest.webmanifest', 'sw.js'].forEach(f => {
  fs.copyFileSync(path.join(root, f), path.join(dst, f));
});

// assets 整目录（含 db.js / shelf.js / app.js / styles.css / 图标）
const srcAssets = path.join(root, 'assets');
const dstAssets = path.join(dst, 'assets');
fs.cpSync(srcAssets, dstAssets, { recursive: true });

// 校验入口存在
const idx = path.join(dst, 'index.html');
if (!fs.existsSync(idx)) { console.error('缺少 index.html'); process.exit(1); }
console.log('www/ 同步完成：', fs.readdirSync(dst).join(', '));
```

### 4.4 `android/app/src/main/java/com/shiguanghe/app/MainActivity.java`（全文）
```java
package com.shiguanghe.app;

import com.getcapacitor.BridgeActivity;

public class MainActivity extends BridgeActivity {}
```

### 4.5 其余源码文件（逐文件路径 + 职责）
| 文件 | 职责 |
|---|---|
| `index.html` | PWA 外壳：顶栏/视图容器/底部 tab/弹窗层/toast，按顺序引入 db.js→shelf.js→app.js |
| `assets/styles.css` | 全部视觉样式（含深色模式、动画、响应式） |
| `assets/db.js` | IndexedDB 封装：`open()` / `seedLocations()`（原子播种，幂等）/ `get`/`getAll`/`put`/`del`/`clear`/`bulkPut`/`setMeta`/`getMeta` |
| `assets/shelf.js` | 业务辅助：`avatar(name)` 按关键字匹配 emoji；`guess(name)` 返回品类保鲜天数建议 |
| `assets/app.js` | 主逻辑：状态计算、首页/录入/清单/详情/拍照/我的/位置/设置/备份/回收桶渲染；日切 `dailyRoll()`；导出导入；全局事件委托 |
| `sw.js` | Service Worker：固定缓存名 `sg-box-v2`，缓存优先，仅缓存同源静态资源 |
| `manifest.webmanifest` | PWA 清单（name=食光盒、图标、display standalone） |

> 以上文件均应使用 UTF-8 读取全量内容；修改后执行 `npm run sync` 同步进 `www/` 与 `android/app/src/main/assets/public/`。

---

## ⑤ 已实现功能清单

- [x] 首页概览：快到期/已过期横幅、物品总数/快到期/已过期/今日新增统计、快捷操作、今日操作流
- [x] 物品录入：名称、数量步进、存放位置（下拉+新增位置）、放入时间、到期日期、照片（压缩 Blob）、语音输入（Web Speech，可插拔）、到期"智能建议"
- [x] 拍照页：相机取帧 / 选图，压缩后带入录入
- [x] 清单：按位置分组、状态标签、筛选（全部/快到期/过期/长期）、名称搜索
- [x] 详情与编辑：查看、编辑、标记"已处理"（入回收桶）、删除
- [x] 位置管理：8 预置家庭分区 + 自定义（重命名/删除）
- [x] 提醒设置：提前天数、到期通知、每日日切、深色模式
- [x] 每日日切快照（daily_log）
- [x] 回收桶：恢复 / 彻底删除
- [x] 本地备份：导出 JSON（照片转 base64 随包）/ 导入恢复
- [x] PWA 可安装 + Service Worker 离线（Web 模式）
- [x] 深色模式 + 减弱动效偏好
- [x] **安卓原生壳（Capacitor）**：工程已生成并修正，可直接构建 APK

---

## ⑥ 已知问题与技术债务

1. **无法在当前开发环境编译 APK**：环境无 Android SDK。安卓工程已生成/修正完毕，最终 `assembleDebug` 需用户本机完成（见 ⑧）。
2. **Service Worker 在原生壳内禁用**：`app.js` 检测到 `window.Capacitor` 时跳过 SW 注册（资源已本地打包，无需 SW 缓存，也规避 WebView 的 SW 兼容问题）。
3. **OCR 与网盘/WebDAV 备份为"可插拔"占位**：设计文档把它们列为可插拔项，当前未实现具体适配器，仅保留接口与说明文案。
4. **相机/语音依赖安全上下文**：Web 模式在 http 局域网下相机/语音不可用（可用"选图"替代拍照，录入不受影响）。
5. **`cap add android` 在沙箱内报错**：生成时因 Windows 回收站权限（safe-delete 拦截）中断，已手动修正 `build.gradle`(namespace/applicationId)、`MainActivity.java`(package)、`strings.xml`(app_name/package_name)、并把 web 资源拷入 `assets/public`。**在用户本机正常环境下 `npm run sync`/`cap sync` 可正常工作**，无需再手动改。
6. **minSdk = 24**：安卓 7.0 以下设备无法安装。
7. **单测缺失**：仅有一次性 jsdom + fake-indexeddb 冒烟测试（验证过播种/录入/清单渲染），无回归测试套件。
8. **`seedLocations` 曾并发重复写入**：已改为单事务内检查+插入（幂等），见 `db.js`。

---

## ⑦ 编码规范约定

- **零外部依赖**：Web 层不引入任何 CDN/框架；新增功能优先原生 API。
- **模块划分**：数据层(`db.js`) ↔ 业务辅助(`shelf.js`) ↔ UI(`app.js`)，三者通过全局函数/对象通信（IIFE 内 `DB`/`SHELF` 命名空间）。
- **安全第一**：所有用户文本插入 DOM 必须经 `esc()` 转义；照片统一压缩后再存储，避免体积膨胀。
- **体积纪律**：任何改动不得显著增加应用壳体积；照片 ≤480px JPEG 0.6；不引入会拖慢冷启动的逻辑。
- **状态判定**：集中在 `statusOf()`，全应用统一口径，勿在多处重复计算。
- **中文界面**：UI 文案中文；代码注释中文，保持与现有风格一致。
- **数据不删原则（临期/过期）**：只提醒、入回收桶，绝不自动清除物品。
- **命名**：文件小写下划线；JS 函数小驼峰；安卓包名 `com.shiguanghe.app`。

---

## ⑧ 下一步开发计划

**A. 产出可安装 APK（当前阻塞项，用户本机执行）**
1. 安装 Android Studio（或 Android SDK + 命令行工具），确保有 SDK Platform 36 与 Build-Tools。
2. 在项目目录执行 `npm install`（若尚未安装），再 `npm run sync`（同步最新 web 资源）。
3. 打开 `android/` 工程：`npx cap open android`（或 Android Studio 打开 `android/` 文件夹）。
4. 等待 Gradle 同步完成，点击 **Build → Build Bundle(s) / APK(s) → Build APK(s)**。
5. 产物：`android/app/build/outputs/apk/debug/app-debug.apk`。传到手机开启"未知来源"即可安装。
6. 若要上架/发布正式版：配置 `signingConfigs` 签名，构建 release AAB。
> 命令行等价：`cd android && ./gradlew assembleDebug`（需 `JAVA_HOME` 与 `ANDROID_HOME` 指向 SDK）。

**B. 功能补全**
- 实现 OCR 适配器（拍照识别物品名/日期），优先以 Capacitor 插件或 TFLite/ML Kit 形式接入，保持可插拔、默认关闭。
- 实现网盘/WebDAV 备份适配器（百度网盘 / WebDAV / 自有云），复用现有导出/导入结构。
- 原生推送（Capacitor Push / Firebase）替代 Web Notification，提升提醒触达。

**C. 体验与发布**
- 用品牌图标替换默认 launcher 图标（生成 mipmap-hdpi…xxxhdpi 各密度）。
- 增加启动屏(Splash)与首次引导。
- 补充自动化测试（Vitest + fake-indexeddb 覆盖日切/状态/备份）。
- 评估上架 Google Play（Capacitor 工程可直接构建 AAB）。

**D. 维护**
- 每次 Web 改动后跑 `npm run sync`；升级 Capacitor 时同步 `cap sync` 并复核包名/资源。
- 缓存名 `sg-box-vN` 在 `sw.js` 与文档中同步递增，避免旧版残留。
